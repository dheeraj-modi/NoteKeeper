<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Note</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100">

    <!-- Navbar -->
    <nav class="bg-gradient-to-r from-blue-600 to-purple-600 backdrop-blur-sm shadow-lg sticky top-0 z-50">
        <div class="max-w-7xl mx-auto px-6">
            <div class="flex justify-between items-center h-16">
                <!-- Logo and Title -->
                <div class="flex-shrink-0">
                    <a href="/" class="flex items-center space-x-3">
                        <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"/>
                        </svg>
                        <span class="font-bold text-2xl text-white hover:text-blue-200 transition-all duration-500">Note Keeper</span>
                    </a>
                </div>
                <h1 class="text-white font-semibold text-[20px]">A Simple Note Making App 😎</h1>
                <h2 class="text-[29px]">📗✍️📙</h2>
            </div>
        </div>
    </nav>


    <!-- Add Note Section -->
    <div class="container mx-auto mt-8 px-4">
        <div class="bg-white p-6 rounded-lg shadow-lg max-w-3xl mx-auto">
            <h2 class="text-3xl font-bold text-gray-800">Edit Note</h2>
            <form action="/edit/<%= filename%>" method="post" class="mt-6">
                <input type="text" name="filename" placeholder="Note Title" value="<%= filename%>" class="w-full px-4 py-2 mb-4 border rounded-lg" required>
                <textarea name="content" placeholder="Note Content" class="w-full px-4 py-2 mb-4 border rounded-lg" required><%= content %></textarea>
                <button type="submit" class="w-full px-4 py-2 bg-green-500 text-white rounded-lg hover:bg-green-600">Save Note</button>
            </form>
        </div>
    </div>

</body>
</html>
